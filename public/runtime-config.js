window.SLACK_GRAPH_CONFIG = {
  // Optional: point to your running backend (for Slack auth/live graph) when hosting this UI on GitHub Pages.
  // Example: apiBase: "https://your-backend.example.com"
  apiBase: ""
};
